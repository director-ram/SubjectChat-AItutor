# Optional ML dependencies (install only when you start training/embedding).
# Note: PyTorch wheels are large; follow official install guidance if needed.

transformers==4.48.2
peft==0.14.0
accelerate==1.3.0
bitsandbytes==0.45.1; platform_system != "Windows"

sentence-transformers==3.3.1
numpy==2.2.2
scikit-learn==1.6.1

# If using OpenAI-compatible SDK directly from Python:
openai==1.60.2
